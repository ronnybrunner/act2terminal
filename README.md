# Actionplan Wizard

Small terminal UI (TUI) that asks for a number (1..50) and generates an Actionplan checklist.

## Run

```bash
npm install
npm start
```

Or install globally / use via `npx` once published:

```bash
actionplan-wizard
```
